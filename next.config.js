/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'avatars.githubusercontent.com'] //need this for images from google and github
    }
}

module.exports = nextConfig
